# pre-push hook
# Trunk-based workflow: this project maintains everything on `main`
# (see docs/CORE_RULES.md Rule 5). No branch is blocked from pushing.
# Kept as a hook seam for future checks (e.g. run tests before push).
exit 0
